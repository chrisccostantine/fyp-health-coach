# External Workout Data

To use the Mega Gym exercise CSV instead of the built-in workout catalog, either:

- place the CSV here as `megaGymDataset.csv`, or
- set `WORKOUT_CSV` to the absolute CSV path.

Expected columns include `Title`, `Desc`, `Type`, `BodyPart`, `Equipment`,
`Level`, `Rating`, and `RatingDesc`.
